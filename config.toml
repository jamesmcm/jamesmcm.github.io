# The URL the site will be built for
base_url = "https://jamesmcm.github.io"

title = "James McMurray's blog"
description = "A technical blog about Rust, Linux and other topics."

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false


taxonomies = [
    {name = "categories", feed = true},
]

# Atom feed
generate_feeds = true
# feed_filenames = "atom.xml"
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
octo_title = "James McMurray's Blog"
octo_domain = "jamesmcm.github.io"
octo_subtitle = "Rust, Linux and other curiosities"
octo_author = "James McMurray"
